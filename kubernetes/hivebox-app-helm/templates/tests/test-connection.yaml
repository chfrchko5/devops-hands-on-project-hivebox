apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Chart.Name }}-test"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Chart.Name }}-svc:{{ .Values.ports.app.httpPort }}/version']
  restartPolicy: Never